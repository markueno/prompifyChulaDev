logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.vscode/*
.vscode/launch.json
!.vscode/extensions.json
.idea
.claude/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/.history
/.cache
/build
.env.local
.env
.env.production
# Catch-all so a stray env file (staging.env, prod.env, …) can't be committed by `git add -A`.
# .env.example is force-added, and is the only one that should ever be tracked.
*.env
!.env.example
.dev.vars
*.vars
.wrangler
_worker.bundle

Modelfile
modelfiles

# docs ignore
site


# one-off scripts (not part of the repo)
scripts/backfill-free-tokens.cjs

# commit file ignore
app/commit.json
changelogUI.md
docs/instructions/Roadmap.md
.cursorrules
*.md
!app/lib/design-md/*.md
graphify-out/
